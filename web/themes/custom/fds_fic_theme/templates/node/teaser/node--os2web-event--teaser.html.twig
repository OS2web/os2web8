
{%
  set classes = [
  'element-wrapper-link',
  node.bundle|clean_class,
  node.isPromoted() ? 'is-promoted',
  node.isSticky() ? 'is-sticky',
  not node.isPublished() ? 'is-unpublished',
  view_mode ? view_mode|clean_class,
]
%}
{% set pageTeaserText = content.field_event_resume is empty
  ? content.body|render
  : content.field_event_resume|render %}

<a href="{{ path('entity.node.canonical', {'node': node.id}) }}" class="news-teaser" aria-label="{{ 'Read more about' | trans }} {{ label }}">
  <i class="fa-solid fa-arrow-right"></i>
  <div class="news-teaser-body">
    {{ content.field_os2web_news_heading }}
    <div class="news-teaser-text">
      {{ pageTeaserText | striptags }}
    </div>
      <div class="my-breadcrumbs">
          <div class="breadcrumb-item">
            Begivenheder
          </div>
      </div>
  </div>
</a>

