{%
  set classes = [
  'section',
  'section--alert',
  'block',
  'block-' ~ configuration.provider|clean_class,
  'block-' ~ plugin_id|clean_class,
]
%}
<div{{ attributes.addClass(classes) }}>
  <div class="container">
    <div class="custom-alert">
      <div class="custom-alert__icon">
        <img src="/{{ base_path ~ directory }}/dist/images/alert.png" alt="Advarsel ikon" />
      </div>
      <div class="custom-alert__meta">
        <h2 class="alert-heading">{{ content.field_os2web_alert_page_ref['#title'] }}</h2>
        {{ content.field_os2web_alert_page_ref['#title'].entity.field_os2web_system_page_content.value }}
      </div>
      <div class="custom-alert__button">
        {{ content.field_link }}
      </div>
    </div>
  </div>
</div>
