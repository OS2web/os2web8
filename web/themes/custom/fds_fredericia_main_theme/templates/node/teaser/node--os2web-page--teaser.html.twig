{%
  set classes = [
  'element-wrapper-link',
  node.bundle|clean_class,
  node.isPromoted() ? 'is-promoted',
  node.isSticky() ? 'is-sticky',
  not node.isPublished() ? 'is-unpublished',
  view_mode ? view_mode|clean_class,
]
%}
{#<div class="page-teaser-wrapper">
  <i class="fa-solid fa-arrow-right"></i>
  <a href="{{ url }}" {{ attributes.addClass(classes) }} aria-label="{{ 'Read more about' | trans }} {{ node.field_os2web_page_heading.value }}">

    <div class="entity-{{ view_mode|clean_class }}__body">
      {{ content.field_os2web_page_heading }}
      {% set text = content.field_os2web_page_description | render %}
      <div class="field field--type-body">
        {{ text | striptags }}
      </div>
    </div>
  </a>
</div>#}
