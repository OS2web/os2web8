{#
/**
 * @file
 * Default theme implementation to display a file field.
 */
#}
{# Debug output #}
<!-- Debug: Template is being used -->
<!-- Element keys: {{ element|keys|join(', ') }} -->
<!-- Has object: {{ element['#object'] is defined ? 'yes' : 'no' }} -->

{% for item in items %}
  <!-- Debug: Item keys: {{ item|keys|join(', ') }} -->
  <!-- Debug: Content keys: {{ item.content|keys|join(', ') }} -->
  {% set node = element['#object'] %}
  {% set document_title = node.field_document_title.value|default('') %}
  <!-- Debug: Document title: {{ document_title }} -->
  
  <div{{ attributes.addClass('field__item') }}>
    <span class="file file--mime-application-pdf file--application-pdf">
      {% if document_title %}
        <a href="{{ item.content['#file'].createFileUrl() }}" type="application/pdf" target="_blank">{{ document_title }}</a>
      {% else %}
        {{ item.content }}
      {% endif %}
    </span>
  </div>
{% endfor %} 