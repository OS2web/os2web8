{#
/**
 * @file
 * Default theme implementation to display a file field.
 */
#}
<!-- Debug: Dumping element structure -->
{{ dump(element) }}

{% for item in items %}
  <div{{ attributes.addClass('field__item test-class') }}>
    {{ item.content }}
  </div>
{% endfor %} 